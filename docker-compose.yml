version: "3.9"

services:
  messaging-service:
    build: .
    container_name: messaging-service
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
